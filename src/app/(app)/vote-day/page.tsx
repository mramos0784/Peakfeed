import ComingSoon from "@/components/ComingSoon";

export default function VoteDayPage() {
  return (
    <ComingSoon
      title="Vote Day"
      roadmapItem={2}
      note="Live results and the vote-night dashboard. Your votes on Lists already count — this is just the dedicated results view."
    />
  );
}
